import { type NextPage } from "next";
import ListPage from "@/features/pull-requests/pages/list-pull-requests.page";

const ListPosts: NextPage = () => {
  return <ListPage />;
};

export default ListPosts;
